@extends('layout')
 
@section('content')
    <h3>お問い合わせリスト</h3>
        <!-- @foreach($lists as $list)
          <hr>
          {{$list->name}}<br>
          {{$list->content}}
          <hr>
        @endforeach -->

        <!-- @foreach($lists as $list)
          <hr>
          {{$list->name}}<br>
          {{$list->content}}
          <hr>
        @endforeach -->

        var_dump($lists);
@endsection